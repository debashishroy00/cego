# CEGO Production Requirements
# Minimal dependencies for production deployment

# Core API framework
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# Production server
gunicorn>=21.2.0

# System monitoring
psutil>=5.9.0

# HTTP client for health checks
requests>=2.31.0

# Week 2 entropy features
numpy>=1.24.0
scipy>=1.10.0

# Note: Quick Wins implementation uses only Python standard library
# for core algorithms. Week 2 entropy features require numpy/scipy.
# Future phases will add:
# - sentence-transformers (for advanced semantic similarity)
# - redis (for caching)
# - prometheus-client (for metrics)